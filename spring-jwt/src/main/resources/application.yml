spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/bus_real_time?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8&useSSL=true
    username: root
    password: 123456
  jpa:
    show-sql: true

mybatis:
  mapper-locations: classpath*:mapper/*.xml
  config-location: classpath:mybatis/mybatis-config.xml

logging:
  level:
    org:
      springframework:
        security: info